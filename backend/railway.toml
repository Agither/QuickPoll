# Railway Deployment Configuration

[build]
builder = "NIXPACKS"

[deploy]
healthcheckPath = "/health"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3

[networking]
serviceDomains = ["quickpoll-production-f532.up.railway.app"]
